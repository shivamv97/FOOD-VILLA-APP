import HeaderComponent from "./Title";
import store from "../utils/store";
const Cart = () => {
  return (
    <div>
      <h1 className="font-bold text-sm">Cart Items</h1>
    </div>
  );
};

export default Cart;
